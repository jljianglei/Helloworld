/*
 *用2个栈实现一个队列，相当于说用2个后进先出的栈实现一个先进先出的队列，思路是
 *比如2栈叫stack1，stack2,队列是queue,插入一个元素a的过程是往stack1里压栈，然后是b,c
 *现在stack1的内容是a,b,c,c在栈顶，是队列最后进的元素，现在考虑从队列中删除一个元素，
 *由于a是先进的，所以应该a先出队列，怎么做呢？现在stack2是空的，我们把a,b,c从stack1中出栈
 *再压入stack2，则stack2的元素是c,b,a,a是栈顶元素，这时stack2出栈，a就出来了，所以说，stack2如果为空，则把stack1出栈，元素
 *依次压入stack2；如果stack2不为空，则stack2的栈顶元素就是队列的队头，stack2出栈就是从队列中删除元素，算法的话就是这样。
 *同样的用2个队列也可以实现一个栈
 *
 *
 */
